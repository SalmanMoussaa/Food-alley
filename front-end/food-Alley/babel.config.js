module.exports = function(api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    

  };
};
import { Platform } from 'react-native';
import { Platform } from 'react-native';

export default {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts'],
};
